import { FundoCianoDirective } from './fundo-ciano.directive';

describe('FundoCianoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoCianoDirective();
    expect(directive).toBeTruthy();
  });
});
